@attribute [Route(Routes.Dashboard)]


<MudStack Row AlignItems="AlignItems.End">
    <MudSpacer />
    <MudButton 
        ButtonType="ButtonType.Submit" 
        Color="Color.Primary" 
        StartIcon="@Icons.Material.Filled.Add" 
        Variant="Variant.Filled" 
        Size="Size.Large" 
        Disabled="false"
        IconSize="Size.Large"
        OnClick="@(() => Console.WriteLine("ADD"))">Add</MudButton>
</MudStack>